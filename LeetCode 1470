class Solution(object):
    def shuffle(self, nums , n):
        temp_List = nums[n:]    # y list
        nums = nums[:n]         # x list
        for i in range(n) :     # put y list to x in 1,3,5..
            nums.insert(2*i+1,temp_List[i])
        return nums             # return them
